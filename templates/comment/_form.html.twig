{{ form_start(formCommentView) }}
    {{ form_widget(formCommentView.rate) }}
    {{ form_widget(formCommentView.comment) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(formCommentView) }}
